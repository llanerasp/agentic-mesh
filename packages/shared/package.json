{
  "name": "@llm-agent/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Tipos y constantes compartidas entre microservicios de llm-agent-mesh.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "lint": "eslint src --max-warnings=0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "rimraf": "^6.0.1",
    "ts-jest": "^29.2.5"
  }
}

apiVersion: v1
kind: Service
metadata:
  name: llm-worker
  namespace: llm-agent
  labels:
    app.kubernetes.io/name: llm-worker
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: llm-worker
  ports:
    - port: 3002
      targetPort: 3002
      name: http

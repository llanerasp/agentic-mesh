apiVersion: v1
kind: Secret
metadata:
  name: llm-worker-secrets
  namespace: llm-agent
type: Opaque
stringData:
  # Vacia por defecto — el provider mock no la necesita.
  # Para activar Claude real: cambiar LLM_PROVIDER=anthropic en el configmap
  # y rellenar aqui con una key real (en produccion, via secret manager).
  ANTHROPIC_API_KEY: ''

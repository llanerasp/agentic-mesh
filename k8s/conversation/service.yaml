apiVersion: v1
kind: Service
metadata:
  name: conversation
  namespace: llm-agent
  labels:
    app.kubernetes.io/name: conversation
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: conversation
  ports:
    - port: 3001
      targetPort: 3001
      name: http

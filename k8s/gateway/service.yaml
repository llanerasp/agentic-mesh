apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: llm-agent
  labels:
    app.kubernetes.io/name: gateway
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: gateway
  ports:
    - port: 3000
      targetPort: 3000
      name: http
